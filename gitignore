# Flutter/Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/pubspec.lock

# Platform folders — generated by `flutter create`, safe to regenerate.
# Uncomment these two if you'd rather NOT commit them (leaner repo, but
# `flutter create --platforms=android,windows .` must be re-run on a
# fresh clone before building). Left commented so a first-time clone
# builds immediately without an extra setup step.
# /android/
# /windows/

# iOS/macOS (not used yet, but standard to exclude)
**/ios/Flutter/.last_build_id
**/ios/Pods/
**/ios/.symlinks/
**/macos/Flutter/ephemeral/

# IDE
.idea/
*.iml
*.ipr
*.iws
.vscode/

# OS
.DS_Store
Thumbs.db

# Coverage
coverage/

# Symbolication
app.*.symbols

# Obfuscation
app.*.map.json

# Android Studio / Gradle local config (machine-specific paths)
**/android/local.properties
**/android/**/GeneratedPluginRegistrant.java
**/android/key.properties
*.jks
*.keystore
